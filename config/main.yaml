defaults:
  - process: process1
  - model: model1
  - _self_

data:
  path: data/
  test_image: test_image.png
  extensions: ['.jpg', '.jpeg', '.png', '.bmp']
  max_display: 5

fractal:
  box_sizes: [2, 4, 8, 16, 32]

results:
  path: results/
  output_file: fractal_analysis_results.csv

display:
  theme: monokai
  spinner: point
  verbosity: 0  # 0=minimal, 1=normal, 2=verbose